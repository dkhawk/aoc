# Git Workflow Rule

NEVER push directly to the `main` branch on GitHub.
Always create a dedicated feature or topic branch (e.g., `feat/feature-name` or `docs/update-readmes`), commit changes to that branch, and push to the branch.
